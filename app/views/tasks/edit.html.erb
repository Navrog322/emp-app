<%= content_for(:html_title) { "Editing #{@task.name}" } %>
<div class="row d-flex justify-content-center">
  <div class="col-4">
    <h1><%= "Editing #{@task.name}" %></h1>

    <%= render "form", task: @task %>

    <br>

    <div>
      <%= link_to "Show this task", @task %> |
      <%= link_to "Back to tasks", tasks_path %>
    </div>
  </div>
</div>
