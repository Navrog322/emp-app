<div class="row d-flex justify-content-center">
  <div class="col-4">
    <p style="color: green"><%= notice %></p>

    <h1>Positions</h1>

    <div id="positions">
      <%= render partial: "positions_table", locals: {positions: @positions}%>
    </div>

    <%= link_to "New position", new_position_path %>
  <div class="row d-flex justify-content-center">
<div class="col-4">